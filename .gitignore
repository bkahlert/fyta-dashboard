# Local config — contains your API token, never commit this
.env.local

# Build output
dist/
storybook-static/
node_modules/

# Brainstorm session files
.superpowers/

# IDE
.idea/

# macOS
.DS_Store
.vite/

# Local-only drift / scratch notes (not part of the workflow itself)
.github/workflows/_DRIFT_NOTES.md
